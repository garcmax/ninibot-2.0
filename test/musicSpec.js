'use strict';
const should = require("should");
const sinon = require("sinon");

import * as music from "../src/music/music.js";

describe ('music class', function () {

  it('should xxx', function (done) {
    let chan = [{type:"voice", name:"Music"},{type:"text", name:"music"}]
    done();
  });

});
