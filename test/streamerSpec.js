'use strict';
const should = require("should");
const sinon = require("sinon");

import Streamer from "../src/musicHelper/streamer.js";

describe ('streamer class helper', function () {

  it('should ', function (done) {

    done();
  });

});
